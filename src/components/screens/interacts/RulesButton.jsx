import styled from 'styled-components';
import { Button } from '../../shared/Button';

export const RulesButton = styled(Button)`
  position: absolute;
  bottom: calc(-40px - 5.3vw);
  left: 50%;
  transform: translateX(-50%);
`;
